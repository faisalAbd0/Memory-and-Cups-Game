using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class customlvl5mng : MonoBehaviour
{
    // Start is called before the first frame update
    public static int attempt = 0;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
